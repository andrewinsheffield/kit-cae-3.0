# Normalize text files on commit to LF endings by default
* text=auto
# Make sure Windows batch files preserve CR/LF line endings, otherwise they may not be able to execute.  Windows
# batch files require a CR/LF for labels to work properly, otherwise they may fail when labels straddle 512-byte
# block boundaries.  This is important when files are downloaded through a zip archive that was authored on a
# Linux machine (the default behavior on GitHub)
*.bat text eol=crlf
*.cmd text eol=crlf
# Make sure shell scripts have LF line endings, even when checked out on a Windows client with autocrlf=true
*.sh text eol=lf
# All files under data go into Git LFS
data/** filter=lfs diff=lfs merge=lfs -text
# Keep the axisymmetric FLASH fixture's documentation, descriptor, and
# generator reviewable; only its generated HDF5 payloads belong in LFS.
data/FLASH/axisymmetric_wavelet/README.md !filter !diff !merge text
data/FLASH/axisymmetric_wavelet/axisymmetric_wavelet.flash !filter !diff !merge text
data/FLASH/axisymmetric_wavelet/generate.py !filter !diff !merge text
source/extensions/**/data/** filter=lfs diff=lfs merge=lfs -text
source/legacy_extensions/**/data/** filter=lfs diff=lfs merge=lfs -text
templates/extensions/**/data/** filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
# Exclude colormaps from LFS
source/legacy_extensions/omni.cae.material_library/materials/colormaps/*.png !filter !diff !merge

*.exr filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.usd filter=lfs diff=lfs merge=lfs -text
*.hdr filter=lfs diff=lfs merge=lfs -text
*.nvdb filter=lfs diff=lfs merge=lfs -text

# XAC files are always text files
*.xac text=auto !filter !merge !diff

# .vti files are always binary
*.vti binary

# USDA are always text files
*.usda text=auto !filter !merge !diff

# treat .case files as text
*.case text=auto !filter !merge !diff
