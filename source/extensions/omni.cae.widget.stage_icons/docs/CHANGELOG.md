# Changelog

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.2.0]

- Added `OmniSciDataset` stage icon (light and dark themes) so USD-plugin-backed CAE datasets get a
  distinct icon in the Stage panel.

## [1.1.0]

- Added `CaeNameColumnDelegate` to add an annotation icon to any prim with `CaeVizOperator` API
  applied to it.

## [1.0.0]

- Released
