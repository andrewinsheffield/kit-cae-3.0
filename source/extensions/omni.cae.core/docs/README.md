# Omni CAE Core

Shared Python runtime utilities for active CAE extensions.

This extension intentionally excludes the legacy Data Delegate and
`IFieldArray` APIs that remain in `source/legacy_extensions/omni.cae.data`.
